import React from 'react';

import { CookieConsent } from '@/components';

const CookieConsentWrapper = ({ cookiesConsentData }) => {
  return <CookieConsent cookiesConsentData={cookiesConsentData} />;
};

export default CookieConsentWrapper;
